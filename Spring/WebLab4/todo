1) как работает ExceptionHandlers + как передать в него инстанс ошибки
2) в чем разница между @Component и @Bean
3) как генерировать методы в *Repository ( с точки зрения внутрненней работы ) + почему не помечаем @Repository
4) UserDetailsService зачем нужен и гшде используется в SS
